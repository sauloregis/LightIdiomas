@model List<LightIdiomas.Entities.Clientes>

<!DOCTYPE html>
<html lang="pt-BR" class="h-full">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard - LightIdiomas</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full flex font-sans bg-gray-100">

  <!-- Menu lateral -->
  <nav class="w-56 bg-blue-900 text-white flex flex-col p-6">
    <h2 class="text-2xl font-bold mb-6">LightIdiomas</h2>
    <a href="#" class="mb-3 font-semibold hover:underline cursor-pointer">Dashboard</a>
    <a href="#" class="mb-3 font-semibold hover:underline cursor-pointer">Alunos</a>
    <a href="#" class="mb-3 font-semibold hover:underline cursor-pointer">Cursos</a>
    <a href="#" class="font-semibold hover:underline cursor-pointer">Configurações</a>
  </nav>

  <!-- Conteúdo principal -->
  <main class="flex-1 p-6 overflow-auto">

    <!-- Dashboard cards -->
    <div class="flex gap-6 mb-6">
      <div class="bg-white rounded-lg p-6 shadow flex-1">
        <h3 class="text-lg font-semibold mb-2">Total de Alunos</h3>
        <p id="totalAlunos" class="text-3xl font-bold">0</p>
      </div>
      <div class="bg-white rounded-lg p-6 shadow flex-1">
        <h3 class="text-lg font-semibold mb-2">Alunos com Whatsapp</h3>
        <p id="alunosWhatsapp" class="text-3xl font-bold">0</p>
      </div>
      <div class="bg-white rounded-lg p-6 shadow flex-1">
        <h3 class="text-lg font-semibold mb-2">Alunos por Nacionalidade</h3>
        <ul id="alunosPorNacionalidade" class="list-disc list-inside"></ul>
      </div>
    </div>

    <!-- Botão Cadastrar -->
    <a href="/CadastrarCliente/CadastrarCliente"
      class="mb-6 inline-block bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition-colors">
      Cadastrar Novo Aluno
    </a>

    <!-- Tabela -->
    <div class="overflow-x-auto bg-white rounded-lg shadow">
      <table class="min-w-full table-auto border-collapse">
        <thead class="bg-blue-600 text-white">
          <tr>
            <th class="px-4 py-3 text-left">Nome</th>
            <th class="px-4 py-3 text-left">Nacionalidade</th>
            <th class="px-4 py-3 text-left">Endereço</th>
            <th class="px-4 py-3 text-left">RG</th>
            <th class="px-4 py-3 text-left">CPF</th>
            <th class="px-4 py-3 text-left">Profissão</th>
            <th class="px-4 py-3 text-left">Email</th>
            <th class="px-4 py-3 text-left">Telefone</th>
            <th class="px-4 py-3 text-left">Whatsapp</th>
          </tr>
        </thead>
        <tbody id="tabelaAlunosBody" class="divide-y divide-gray-200">
          @foreach (var cliente in Model)
          {
            <tr>
              <td class="px-4 py-3">@cliente.Nm_Cliente</td>
              <td class="px-4 py-3">@cliente.Nacionalidade</td>
              <td class="px-4 py-3">@cliente.Endereco</td>
              <td class="px-4 py-3">@cliente.RG</td>
              <td class="px-4 py-3">@cliente.CPF</td>
              <td class="px-4 py-3">@cliente.Profissao</td>
              <td class="px-4 py-3">@cliente.Email</td>
              <td class="px-4 py-3">@cliente.Telefone</td>
              <td class="px-4 py-3">@(cliente.Whatsapp ? "Sim" : "Não")</td>
            </tr>
          }
        </tbody>
      </table>
    </div>

  </main>

  <script src="index.js" type="module"></script>
</body>
</html>
